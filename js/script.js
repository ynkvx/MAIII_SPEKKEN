(() => {

	//   const clickHandler = e => {
	// 		if(!e.target.value) alert('Please Select One');
	//     // else alert('You like ' + e.target.value + ' bro');
	// 		let $filter = document.querySelector('.filter');
	// 		$filter.submit();
	//   }
	//
	// const init = () => {
	// 	document.querySelector('.filter-item').addEventListener("change", clickHandler);
		// document.querySelector('.filter-input').addEventListener("input", clickHandler);
	};

	init();

})();
